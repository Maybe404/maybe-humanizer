# 进度记录

按 02-plan.md 第 8 节的阶段表对照。每次阶段状态变化时更新本文件，条目按日期倒序。

## 当前状态（2026-09-07）

| 阶段 | 完成定义 | 状态 |
|---|---|---|
| 0 | 两份基线数据入库 | 未达标。契约修正已合入前一版仓库；两份基线没跑 |
| 1 | 台账编号零缺失；逐字项零丢失；与两份基线在同一批样本上对照，误改与越界有下降证据 | 未达标。代码写完、单元测试通过、5 条用例真跑通过；对照没做，因为基线没有 |
| 2 | 漏检相对阶段 1 下降；误报率有数且有标注支撑；skill/ 可安装 | 未开始 |
| 3 | 按需 | 未开始 |

### 已固定的版本

| 对象 | 位置 | commit |
|---|---|---|
| 前一版 skill，原版 | `github.com/Maybe404/skill` | `37e96bf` |
| 前一版 skill，修正版（04 的四处矛盾加顺带措辞） | 同上 | `991c382` |
| SOURCES.md 渲染脚本修复（04 第 5 节） | 同上 | `89d6dae` |
| 本仓库原型 | `github.com/Maybe404/maybe-humanizer` | `6e7b921` |

### 阶段 0 明细

- 修契约：完成。18 处文字替换，脚本 `scripts/phase0_fixes.py` 可重复执行，`--check` 报状态。
- 留出样本：4 条中文在 `evals/holdout/`，未参与开发。
- 原版基线：未跑。
- 修正版基线：未跑。
- 跑法与记录格式：`evals/baselines/README.md`、03-formats.md「基线记录」。

### 阶段 1 明细

已完成：

- 引擎 `engine/`：门检与占位（protect）、按句编号（text）、逐字项与语义项抽取和分诊（prepare）、句子级补丁（patch）、核验（verify）、语义判定输入（judge）、报告（report）、编排与 CLI（pipeline、cli）。
- prompts：诊断、改写、语义判定三份，另有 structural、bounded、in-place 三个范例。
- rules：13 条代表规则，schema 暂定，`severity` 与 `frequency` 已预留。
- lexicons：`zh.yaml`，只供核验计数与复扫，不是扫描器。
- evals：17 条中文用例结构化到 `evals/cases/`（id 沿用前一版 1、2、5、6、9、10、13–22、26），运行器 `evals/runner.py`。
- skill：`skill/SKILL.md` 路由，含无引擎的降级路径。
- 测试：21 个单元测试通过，ruff 通过。
- 首次真跑：Sonnet 5，第 6、13、14、20、22 条，机械断言全过，核验均一轮通过，记录在 `evals/records/proto-1-sonnet5/`。

未完成：

- 其余 12 条用例和 4 条留出样本没跑。
- 与两份基线的对照没做。
- 已知问题：语义判定对删整句空话偏保守，把删「充分彰显……工匠精神」「非常有价值的优化尝试」标为「需作者确认」而不是「通过」。不阻断交付，但 `prompts/judge.md` 里「整句空话判通过」的边界要收紧。

### 运行条件

- 本机 `claude -p` 必须显式传 `--model`，且要 `env -u CLAUDECODE`，否则沿用当前会话的模型设置报错。温度不可控，基线记录里记为不可控。
- 云端跑需要换成 API 后端，`engine/pipeline.py` 的 `Backend` 协议已预留，未实现。

### 成本估算（未实测）

- 基线：前一版 skill 每次调用载入约 60k 到 80k token 规则，17 条乘两份约 240 万输入 token。修正版可以只跑有差异的第 6、9、20、26 条加少量对照。
- 原型：每条三次调用，每次 5k 到 8k 输入，17 条合计约 40 万。
- 人工：34 份基线输出要按 `judge_rubric` 逐条判四类，可让模型先初判、人复核争议项。

## 下一步

1. 跑原版基线，按四类记录入库。
2. 跑修正版基线，确认差异只在预期的几条上。
3. 原型跑完 17 条加 4 条留出样本。
4. 三份数据对照。误改与越界有下降才进阶段 2，没有就改原型。
5. 收紧 judge prompt 对整句空话的判定边界。
